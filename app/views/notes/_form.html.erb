<%= simple_form_for(@note) do |f| %>
  <%= f.error_notification %>

  <div class="inputs">
    <%= f.input :title %>
    <%= f.input :details %>
  </div>

  <div class="actions">
    <%= f.button :submit %>
  </div>
<% end %>
